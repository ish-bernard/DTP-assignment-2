# Create a list and unpack its values into separate variables 

data =["python", 3.12, True, 500]
lang, version, satus, users = data
print(f"The programming language is {lang} version {version}, status is {satus}, and has over {users} million users.")